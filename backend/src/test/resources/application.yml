spring:
  datasource:
    url: jdbc:h2:mem:eto_crm_tenant;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
eto:
  platform:
    datasource:
      url: jdbc:h2:mem:eto_crm_platform;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
      username: sa
      password:
