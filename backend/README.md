# Backend

Spring Boot modular-monolith backend for ETO CRM. Run with `mvn spring-boot:run`.

The scaffold deliberately contains no persistence model; domain entities will be introduced through accepted vertical slices.

