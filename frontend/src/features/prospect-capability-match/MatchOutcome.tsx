import type { Match } from './prospectCapabilityMatchTypes'
export function MatchOutcome({result,error}:{result:Match|null;error:string}){return <section><p className="section-label">Save outcome</p>{!result&&!error&&<p>Incomplete match information is safe to save as a draft.</p>}{error&&<p role="alert" className="error">{error}</p>}{result&&<><h2><span className={`status ${result.status.toLowerCase()}`}>{result.status}</span> {result.accountName}</h2>{result.missingInformation.length?<><p>Add these details to activate the match:</p><ul>{result.missingInformation.map(item=><li key={item}>{item}</li>)}</ul></>:<p>Ready for {result.owner}: {result.nextAction}</p>}</>}</section>}
